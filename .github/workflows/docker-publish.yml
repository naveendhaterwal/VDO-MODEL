name: Build and Push to Docker Hub

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Install PyTorch (CPU build for CI)
        run: |
          pip install --upgrade pip
          # Pin to same major version as Dockerfile to avoid compat issues
          pip install torch==2.6.0 torchvision==0.21.0 torchaudio==2.6.0 --index-url https://download.pytorch.org/whl/cpu

      - name: Install dependencies
        run: |
          # bitsandbytes requires CUDA kernels, so remove it for CPU-only CI.
          grep -v '^bitsandbytes==' requirements.txt > requirements-ci.txt
          pip install -r requirements-ci.txt
          pip install pytest pytest-timeout fakeredis

      - name: Run smoke tests
        run: python -m pytest tests/test_pipeline.py tests/smoke_test.py -v --timeout=120

  build-and-push:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Maximize build space
        uses: easimon/maximize-build-space@master
        with:
          remove-dotnet: 'true'
          remove-android: 'true'
          remove-haskell: 'true'
          remove-codeql: 'true'
          remove-docker-images: 'true'

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Generate metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: naveendhaterwal/vimax
          tags: |
            type=sha,prefix=
            type=raw,value=v${{ github.run_number }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.worker
          platforms: linux/amd64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=registry,ref=naveendhaterwal/vimax:buildcache
          cache-to: type=registry,ref=naveendhaterwal/vimax:buildcache,mode=max
          labels: ${{ steps.meta.outputs.labels }}
